export const metas = [
{{#each colors}}
{
  name: "{{this.name}}",
  alias: "{{this.alias}}",
  rgb: {{this.rgb}},
  hex: "{{this.hex}}"
},
{{/each}}
]

export const colors = {
{{#each colors}}
  "{{this.alias}}": "{{this.hex}}",
{{/each}}
}
